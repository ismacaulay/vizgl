
def build(bld):
    bld(
        includes=['glm'],
        export_includes=['.'],
        name='glm',
    )

    bld.stlib(
        source=bld.path.ant_glob('imgui/*.cpp'),
        includes=['imgui'],
        export_includes=['.'],
        target='imgui',
    )
